define(["CanvasLine", "constant", "CanvasManager", "i18n!/nls/lang.js"], function(CeLine, constant, CanvasManager, lang) {
  CeLine.extend({

    /* env:dev                                                env:dev:end */
    type: "MarathonDepOut"
  });
  return CeLine.extend({

    /* env:dev                                               env:dev:end */
    type: "MarathonDepIn"
  });
});
